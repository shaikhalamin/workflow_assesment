/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api/client.ts";
import type {
  ExpensesControllerSubmitMutationResponse,
  ExpensesControllerSubmitPathParams,
  ExpensesControllerSubmit400,
  ExpensesControllerSubmit401,
  ExpensesControllerSubmit403,
  ExpensesControllerSubmit404,
} from "../../types/expensesController/ExpensesControllerSubmit.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/api/client.ts";

function getExpensesControllerSubmitUrl({
  id,
}: {
  id: ExpensesControllerSubmitPathParams["id"];
}) {
  const res = { method: "POST", url: `/api/expenses/${id}/submit` as const };
  return res;
}

/**
 * {@link /api/expenses/:id/submit}
 */
export async function expensesControllerSubmit(
  { id }: { id: ExpensesControllerSubmitPathParams["id"] },
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    ExpensesControllerSubmitMutationResponse,
    ResponseErrorConfig<
      | ExpensesControllerSubmit400
      | ExpensesControllerSubmit401
      | ExpensesControllerSubmit403
      | ExpensesControllerSubmit404
    >,
    unknown
  >({
    method: "POST",
    url: getExpensesControllerSubmitUrl({ id }).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
