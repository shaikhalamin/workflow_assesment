/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/lib/api/client.ts";
import type {
  LeavesControllerSubmitMutationResponse,
  LeavesControllerSubmitPathParams,
  LeavesControllerSubmit400,
  LeavesControllerSubmit401,
  LeavesControllerSubmit403,
  LeavesControllerSubmit404,
} from "../../types/leavesController/LeavesControllerSubmit.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/api/client.ts";

function getLeavesControllerSubmitUrl({
  id,
}: {
  id: LeavesControllerSubmitPathParams["id"];
}) {
  const res = { method: "POST", url: `/api/leaves/${id}/submit` as const };
  return res;
}

/**
 * {@link /api/leaves/:id/submit}
 */
export async function leavesControllerSubmit(
  { id }: { id: LeavesControllerSubmitPathParams["id"] },
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    LeavesControllerSubmitMutationResponse,
    ResponseErrorConfig<
      | LeavesControllerSubmit400
      | LeavesControllerSubmit401
      | LeavesControllerSubmit403
      | LeavesControllerSubmit404
    >,
    unknown
  >({
    method: "POST",
    url: getLeavesControllerSubmitUrl({ id }).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
